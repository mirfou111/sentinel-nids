# 🛡️ SENTINEL — Network Intrusion Detection System

Système de détection d'intrusion réseau en temps réel
avec détection ML des port scans et brute force.

## Lancement
```bash
sudo python -m sentinel start
sudo python -m sentinel watch
```
